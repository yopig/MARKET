# --- DB ---
spring.datasource.url=jdbc:mariadb://mydb1.c1y6iegk246m.ap-northeast-2.rds.amazonaws.com:3306/prj04_2
spring.datasource.username=admin

# --- ?? ---
spring.application.name=backend
spring.config.import=classpath:secret/custom.properties

# --- JPA ---
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false
spring.jpa.hibernate.ddl-auto=validate

# --- ???/?? ---
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=200MB
spring.servlet.multipart.enabled=true
server.tomcat.max-http-form-post-size=200MB
server.tomcat.max-swallow-size=200MB
server.tomcat.max-connections=8192
server.tomcat.max-part-count=100
server.tomcat.accept-count=1000
server.tomcat.threads.max=400
server.max-http-request-header-size=64KB
server.address=0.0.0.0
server.port=8080

# --- ?? ---
logging.level.org.hibernate.SQL=debug
logging.level.org.hibernate.orm.jdbc.bind=trace

# --- ??: Gmail (? ???? ??) ---
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${gmail.username}
spring.mail.password=${gmail.appPassword}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.debug=true

# --- ??? ?? ---
app.email.from=${gmail.username}
app.email.subject=[????] ??? ?? ??
app.email.mode=mail
app.email.resend-cooldown-minutes=0
spring.mail.default-encoding=UTF-8